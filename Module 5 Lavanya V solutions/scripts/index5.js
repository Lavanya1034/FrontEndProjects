//Assignment 5
//To register employee details

//Get input details of employee

//get the first name
let firstName = window.prompt("Enter the first name");
console.log(firstName);

//get the last name
let lastName = window.prompt("Enter the last name");
console.log(lastName);

//get the Age name
let age = parseInt(window.prompt("Enter the age"));
console.log(age);

//get the Gender
let gender = window.prompt("Enter the gender");
console.log(gender);

//get the contant number
let contactNo = window.prompt("Enter the contact number");
console.log(contactNo);

//get the city
let cit=window.prompt("Enter the city");
console.log(cit);

//output print
console.log(`${firstName} ${lastName} who is a ${gender}, aged ${age} years, lives in ${cit}. ${firstName}’s contact number is ${contactNo}.`);

